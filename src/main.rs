// starting over from scratch because it's been a while since I wrote anything like this

use std::num::NonZeroU32;

async fn run() {
    let instance = wgpu::Instance::new(wgpu::BackendBit::PRIMARY); // default to what ever is the 
}